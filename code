#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
#include<string.h>
struct library{
   char bookname[50];
   char author[50];
   int noofpages;
   float price;
};
int main(){
    system("cls");
           printf("project made by SUMIT JAISWAL(2K21/B17/58)\n");
       printf("                RISHIT SAINI (2K21/B17/46)\n");
       printf("\npresented to respected PRIYA MA'AM (Department of software engg.)\n");
       printf("\n");
       printf("WELCOME TO LIBRARY MANAGEMENT SYSTEM\n");
       printf("**************************************\n");
   struct library lib[100];
   char bookname[30];
   int i,j, keepcount;
   i=j=keepcount = 0;
   while(j!=4){

       printf("\nyou can do the following operation\n");
      printf("\n1. Add book information\n");
      printf("2. Display book information\n");
      printf("3. no of books in the library\n");
      printf("4. Exit");
      printf ("\n\nEnter one of the above : ");
      scanf("%d",&j);
      switch (j){
         /* Add book */
         case 1:
            printf ("Enter book name = ");
            scanf ("%s",lib[i].bookname);
            printf ("Enter author name = ");
            scanf ("%s",lib[i].author);
            printf ("Enter pages = ");
            scanf ("%d",&lib[i].noofpages);
            printf ("Enter price = ");
             scanf ("%f",&lib[i].price);
             printf("\n");
            keepcount++;
            i++;
            break;
         case 2:
            printf("you have entered the following information\n");
            for(i=0; i<keepcount; i++){
                int k=i+1;
               printf ("\n%d. book name = %s\n",k,lib[i].bookname);
               printf ("\t author name = %s\n",lib[i].author);
               printf ("\t pages = %d\n",lib[i].noofpages);
               printf ("\t price = %f\n\n",lib[i].price);
            }
            break;
         case 3:
            printf("\n No of books in library : %d\n", keepcount);
            break;
         case 4:
         printf("\nsee you again\n");
            j=4;
      }
   }
   return 0;
}
